#!/bin/bash
if [[ $# -eq 0 ]]; then
	rm -f .DS_Store
else
	if [[ $# -eq 1 ]] && [[ ${1} == "-h" ]]; then
		echo " Usage: ${0} [dir1 dir2...]"
		echo "   Removes the annoying Mac .DS_Store from . or from dir1 dir2..."
	else
		for i in "${@}"; do
			rm -f ${i}/.DS_Store
		done
	fi
fi
