#!/bin/bash
## Created: TIMESTAMP
## Version: 0.01

set -eu

function #&%() {
	if [[ $# -eq 0 ]] || [[ [[ $# -eq 1 ]] && [[ $1 == "-h" ]] || [[ $1 == "--help" ]] ]]; then
		## Display help, explain usage
		#&%
		return 0;
	fi
	#&%

	return 0;
}
