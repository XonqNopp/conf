! Memo: when using -fopenmp, lines beginning by '!$' are also executed
!       use omp_lib
!       omp_set_num_threads(n)
!       omp parallel do private(var_x)
!
! TODO:
!
      program progname
           !libs
           implicit none
           !vars
           !commands
      end program progname
